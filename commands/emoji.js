module.exports.run = async (client, msg, args) => {

    msg.channel.send("<a:pepeClap:513191615339298820>")
}

module.exports.config = {
    name: 'emoji',
    aliases: ['emote'], // Even if you don't want an alias, leave this as an array.
};